apiVersion: v1
kind: Pod
metadata:
  name: nginx-default
  namespace: default
spec:
  containers:
  - image: nginx
    imagePullPolicy: Always
    name: nginx
    ports:
    - containerPort: 80
      protocol: TCP
